<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/style.css">
        <title>Anime Shows</title>
    </head>
    <body>
        <!-- <form action="/smash/<%=characters[i].id; %>?_method=DELETE" method="POST">
                        <input type="submit" value="DELETE"/>
                    </form>
                </td>
                <th>
                    <a href="/smash/<%=characters[i].id%>"><%=characters[i].name%></a>
                </th>
                <td><a href="/smash/<%=characters[i].id; %>/edit">Edit</a></td>
                <td><img src ="<%=characters[i].img; %>"></td>
                <td><%=characters[i].origin%></td>
            </tr>
            <% } %>
        </table> -->
    </body>
</html>